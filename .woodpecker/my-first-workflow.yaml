when:
  - event: push
    branch: woodpecker-test
    

steps:
  - name: build
    image: debian
    commands:
      - echo "This is the build step"
  - name: a-test-step
    image: eclipse-temurin:8
    commands:
      - mvn clean package