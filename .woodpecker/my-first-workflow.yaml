when:
  - event: push
    branch: woodpecker-test
  - event: manual
    branch: woodpecker-test
    

steps:
  - name: build
    image: debian
    commands:
      - echo "This is the build step"
  - name: a-test-step
    image: eclipse-temurin:8
    commands:
      - bash ./gradlew build --no-cache